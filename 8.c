#include <stdio.h>

int main(void)
{
	int numb;

	for (numb = 48; numb <= 57; numb++)
		putchar(numb);

			int letter;

	for (letter = 'a'; letter <= 'f'; letter++)
		putchar(letter);

	putchar('\n');

	return (0);

}
